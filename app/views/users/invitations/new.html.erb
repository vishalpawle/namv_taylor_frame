<h2><%= t "devise.invitations.new.header" %></h2>
</br>
<strong> Only Admin can invite </strong>
</br>
<strong> Only Teacher can be invited from here </strong>
</br>
<strong> I am assigning "teacher role" through backend process you just enter his / her email and do not make any changes here </strong>
<%= simple_form_for(resource, as: resource_name, url: invitation_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.error_notification %>

  <% resource.class.invite_key_fields.each do |field| -%>
    <div class="form-inputs">
      <%= f.input field %>
      <%= f.hidden_field :role, :value => "teacher" %>
    </div>
  <% end -%>

  <div class="form-actions">
    <%= f.button :submit, t("devise.invitations.new.submit_button") %>
  </div>
<% end %>
